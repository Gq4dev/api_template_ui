{#- Ported from legacy email/cvu_transaction_reversed.ftl.
    NOTE: the legacy source file was empty (0 bytes) in the defaultV2 theme, so there is no
    markup, copy, or payload to port. This stub extends the base layout with default (empty)
    blocks. Populate the content block once the legacy template is defined. -#}
{% extends "base.html.j2" %}
